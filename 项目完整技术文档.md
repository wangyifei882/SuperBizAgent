# SuperBizAgent 项目完整技术文档

---

## 目录
1. [项目概述](#一项目概述)
2. [项目架构](#二项目架构)
3. [核心功能实现](#三核心功能实现)
4. [API 接口文档](#四api接口文档)
5. [配置说明](#五配置说明)
6. [关键细节](#六关键细节)
7. [常见问题](#七常见问题)
8. [扩展方向](#八扩展方向)

---

## 一、项目概述

### 1.1 项目定位
**SuperBizAgent** 是一个完整的 **AIOps 智能运维系统**，旨在利用 AI Agent 技术自动化解决运维人员日常告警排查工作。项目结合了 RAG（检索增强生成）、大语言模型、多 Agent 协作等技术，形成了一个完整的智能运维解决方案。

### 1.2 项目目标
- **提高效率**：自动化告警排查，降低人工介入时间
- **提升准确性**：基于知识库和历史数据，减少误判
- **学习友好**：代码结构清晰，适合学习 Spring AI Agent
- **可扩展**：模块化设计，方便添加新功能

### 1.3 核心功能
| 功能 | 说明 |
|------|------|
| **RAG 智能问答** | 从运维知识库检索相关文档，结合大模型生成准确回答 |
| **AIOps 智能运维** | 多 Agent 协作，自动查询告警、日志、文档，生成结构化分析报告 |
| **文档管理** | 支持上传文档，自动分片和向量化，存储到向量数据库 |
| **会话管理** | 支持多会话，记忆对话历史，上下文感知 |
| **流式输出** | SSE 实时流式推送，提升用户体验 |

### 1.4 技术栈
| 技术 | 版本/说明 | 用途 |
|------|-----------|------|
| **Java** | 17 | 主要编程语言 |
| **Spring Boot** | 3.2.0 | Web 框架 |
| **Spring AI** | 1.1.0 | AI 集成框架 |
| **Spring AI Alibaba** | 1.1.0.0-RC2 | 阿里云 DashScope 集成 |
| **Milvus** | 2.6.10 | 向量数据库 |
| **阿里云 DashScope** | - | 大语言模型 API |
| **text-embedding-v4** | - | 文本向量化模型 |
| **qwen3-max** | - | 主对话大模型 |
| **qwen3-30b-a3b-thinking-2507** | - | RAG 专用思考模型 |

---

## 二、项目架构

### 2.1 整体架构图
```
前端 (HTML/JS/CSS)
  │
  ├── 普通对话界面 ──→ /api/chat_stream (SSE)
  └── 智能运维界面 ──→ /api/ai_ops (SSE)
         │
         ▼
┌─────────────────────────────────────────────────┐
│              Spring Boot Controller              │
│  ChatController.java                             │
│  FileUploadController.java                       │
│  MilvusCheckController.java                      │
└──────────┬──────────────────────┬────────────────┘
           │                      │
           ▼                      ▼
    ChatService.java        AiOpsService.java
   (单 Agent React)        (多 Agent 协作编排)
           │                      │
           │              ┌───────┴────────┐
           │              ▼                ▼
           │    Supervisor Agent    Planner Agent
           │    (调度器)            (任务规划)
           │         │                   │
           │         └───────┬───────────┘
           │                 ▼
           │          Executor Agent
           │          (任务执行)
           │                 │
           ▼                 ▼
┌─────────────────────────────────────────────────┐
│                    Agent 工具层                   │
│  DateTimeTools │ InternalDocsTools │             │
│  QueryMetricsTools │ QueryLogsTools │ MCP Tools  │
└──────────┬──────────────────────┬────────────────┘
           │                      │
           ▼                      ▼
    RagService.java         阿里云 DashScope
    ├── VectorSearchService   (LLM API)
    ├── VectorIndexService
    ├── VectorEmbeddingService
    └── DocumentChunkService
           │
           ▼
    Milvus 向量数据库
    Prometheus/CLS (可选)
```

### 2.2 模块划分
| 模块 | 包路径 | 职责 |
|------|--------|------|
| **Controller** | `org.example.controller` | HTTP 接口、会话管理、SSE 流式输出 |
| **Service** | `org.example.service` | 业务逻辑：RAG、Agent 编排、向量化、检索 |
| **Agent Tool** | `org.example.agent.tool` | Agent 可调用的工具 |
| **Config** | `org.example.config` | Spring Boot 配置类 |
| **Client** | `org.example.client` | Milvus 客户端工厂 |
| **DTO** | `org.example.dto` | 数据传输对象 |
| **Constant** | `org.example.constant` | 常量定义 |

### 2.3 文件结构
```
SuperBizAgent/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── org/example/
│   │   │       ├── Main.java
│   │   │       ├── controller/
│   │   │       │   ├── ChatController.java
│   │   │       │   ├── FileUploadController.java
│   │   │       │   └── MilvusCheckController.java
│   │   │       ├── service/
│   │   │       │   ├── ChatService.java
│   │   │       │   ├── AiOpsService.java
│   │   │       │   ├── RagService.java
│   │   │       │   ├── VectorSearchService.java
│   │   │       │   ├── VectorIndexService.java
│   │   │       │   ├── VectorEmbeddingService.java
│   │   │       │   └── DocumentChunkService.java
│   │   │       ├── agent/tool/
│   │   │       │   ├── DateTimeTools.java
│   │   │       │   ├── InternalDocsTools.java
│   │   │       │   ├── QueryMetricsTools.java
│   │   │       │   └── QueryLogsTools.java
│   │   │       ├── config/
│   │   │       │   ├── DashScopeConfig.java
│   │   │       │   ├── MilvusConfig.java
│   │   │       │   ├── MilvusProperties.java
│   │   │       │   ├── DocumentChunkConfig.java
│   │   │       │   ├── FileUploadConfig.java
│   │   │       │   └── WebConfig.java
│   │   │       ├── client/
│   │   │       │   └── MilvusClientFactory.java
│   │   │       ├── dto/
│   │   │       │   ├── DocumentChunk.java
│   │   │       │   ├── AIOpsRequest.java
│   │   │       │   └── FileUploadRes.java
│   │   │       └── constant/
│   │   │           └── MilvusConstants.java
│   │   └── resources/
│   │       ├── application.yml
│   │       └── static/
│   │           └── 前端文件
│   └── test/
├── aiops-docs/
│   └── 运维知识库文档
├── pom.xml
├── Makefile
└── vector-database.yml (Milvus Docker Compose)
```

---

## 三、核心功能实现

### 3.1 RAG 智能问答

#### RAG 原理
RAG（Retrieval-Augmented Generation）即检索增强生成，是一种将检索模块与生成模块结合的技术。它的核心思想是：
1. **存储阶段**：将知识库文档向量化，存储到向量数据库
2. **检索阶段**：将用户提问向量化，在向量数据库中找到最相似的文档片段
3. **生成阶段**：将检索到的文档作为上下文注入到提示词中，让大模型基于这些知识回答问题

#### 完整流程
```
文档上传 (.txt/.md)
  │
  ▼
DocumentChunkService (文档分片)
  ├── 按 \n\n 分割段落
  ├── 每片最大 800 字符（可配置）
  └── 相邻片重叠 100 字符（可配置）
  │
  ▼
VectorEmbeddingService (文本向量化)
  └── 调用阿里云 text-embedding-v4 模型，生成 1536 维向量
  │
  ▼
VectorIndexService (存入 Milvus)
  ├── Collection: biz
  ├── 字段: id, content, vector, metadata
  └── 索引: IVF_FLAT, L2 距离
  │
  ▼
用户提问 ──→ 向量化 ──→ VectorSearchService (检索 Top-K)
  │
  ▼
RagService (构建 Prompt + 流式生成)
  ├── 将检索结果注入上下文
  └── 调用 qwen3-30b-a3b-thinking-2507 流式生成回答
```

#### 关键代码详解

##### 1. 文档分片 - DocumentChunkService

```java
@Service
@Configuration
@EnableConfigurationProperties(DocumentChunkConfig.class)
public class DocumentChunkService {

    private final DocumentChunkConfig config;

    public DocumentChunkService(DocumentChunkConfig config) {
        this.config = config;
    }

    public List<DocumentChunk> chunkDocument(String content) {
        List<DocumentChunk> chunks = new ArrayList<>();
        String[] paragraphs = content.split("\\n\\n");
        StringBuilder currentChunk = new StringBuilder();

        for (int i = 0; i < paragraphs.length; i++) {
            String paragraph = paragraphs[i].trim();
            
            if (paragraph.isEmpty()) {
                continue;
            }

            if (currentChunk.length() + paragraph.length() > config.getMaxChunkSize()) {
                chunks.add(new DocumentChunk(currentChunk.toString()));
                
                // 保留重叠部分，让语义更连贯
                String overlap = currentChunk.substring(
                    Math.max(0, currentChunk.length() - config.getOverlapSize()));
                currentChunk = new StringBuilder(overlap);
            }
            
            currentChunk.append(paragraph).append("\n\n");
        }

        if (!currentChunk.toString().trim().isEmpty()) {
            chunks.add(new DocumentChunk(currentChunk.toString()));
        }

        return chunks;
    }
}
```

**设计要点**：
- 按双换行符分割段落，保持语义完整性
- 相邻片段重叠 100 字符，防止上下文断裂
- 可通过配置调整分片大小和重叠度

##### 2. 向量检索 - VectorSearchService

```java
@Service
public class VectorSearchService {

    private static final Logger logger = LoggerFactory.getLogger(VectorSearchService.class);

    @Autowired
    private MilvusServiceClient milvusClient;

    @Autowired
    private VectorEmbeddingService embeddingService;

    /**
     * 搜索相似文档
     */
    public List<SearchResult> searchSimilarDocuments(String query, int topK) {
        try {
            logger.info("开始搜索相似文档, 查询: {}, topK: {}", query, topK);

            // 1. 将查询文本向量化
            List<Float> queryVector = embeddingService.generateQueryVector(query);
            logger.debug("查询向量生成成功, 维度: {}", queryVector.size());

            // 2. 构建搜索参数
            SearchParam searchParam = SearchParam.newBuilder()
                    .withCollectionName(MilvusConstants.MILVUS_COLLECTION_NAME)
                    .withVectorFieldName("vector")
                    .withVectors(Collections.singletonList(queryVector))
                    .withTopK(topK)
                    .withMetricType(MetricType.L2)  // 使用欧氏距离
                    .withOutFields(List.of("id", "content", "metadata"))
                    .withParams("{\"nprobe\":10}")
                    .build();

            // 3. 执行搜索
            R<SearchResults> response = milvusClient.search(searchParam);

            if (response.getStatus() != 0) {
                throw new RuntimeException("向量搜索失败: " + response.getMessage());
            }

            // 4. 解析搜索结果
            SearchResultsWrapper wrapper = new SearchResultsWrapper(response.getData().getResults());
            List<SearchResult> results = new ArrayList<>();

            for (int i = 0; i < wrapper.getRowRecords(0).size(); i++) {
                SearchResult result = new SearchResult();
                result.setId((String) wrapper.getIDScore(0).get(i).get("id"));
                result.setContent((String) wrapper.getFieldData("content", 0).get(i));
                result.setScore(wrapper.getIDScore(0).get(i).getScore());
                
                Object metadataObj = wrapper.getFieldData("metadata", 0).get(i);
                if (metadataObj != null) {
                    result.setMetadata(metadataObj.toString());
                }
                
                results.add(result);
            }

            logger.info("搜索完成, 找到 {} 个相似文档", results.size());
            return results;

        } catch (Exception e) {
            logger.error("搜索相似文档失败", e);
            throw new RuntimeException("搜索失败: " + e.getMessage(), e);
        }
    }

    /**
     * 搜索结果类
     */
    @Setter
    @Getter
    public static class SearchResult {
        private String id;
        private String content;
        private float score;
        private String metadata;
    }
}
```

**向量检索参数说明**：
- **L2 距离**：欧氏距离，值越小相似度越高
- **IVF_FLAT**：IVF 是倒排文件索引，FLAT 表示暴力搜索，适合中小规模数据
- **nprobe=10**：搜索时检查的聚类中心数量，数量越多精度越高但速度越慢

##### 3. 流式生成 - RagService

```java
@Service
public class RagService {

    private static final Logger logger = LoggerFactory.getLogger(RagService.class);

    @Autowired
    private VectorSearchService vectorSearchService;

    @Value("${dashscope.api.key}")
    private String apiKey;

    @Value("${rag.top-k:3}")
    private int topK;

    @Value("${rag.model:qwen3-30b-a3b-thinking-2507}")
    private String model;

    private Generation generation;

    @PostConstruct
    public void init() {
        Constants.apiKey = apiKey;
        Constants.baseHttpApiUrl = "https://dashscope.aliyuncs.com/api/v1";
        generation = new Generation();
        logger.info("RAG 服务初始化完成, model: {}, topK: {}", model, topK);
    }

    public void queryStream(String question, List<Map<String, String>> history, StreamCallback callback) {
        try {
            logger.info("收到 RAG 流式查询: {}", question);

            // 1. 从向量数据库检索相关文档
            List<VectorSearchService.SearchResult> searchResults = 
                vectorSearchService.searchSimilarDocuments(question, topK);

            callback.onSearchResults(searchResults);

            if (searchResults.isEmpty()) {
                logger.warn("未找到相关文档");
                callback.onComplete("抱歉，我在知识库中没有找到相关信息来回答您的问题。", "");
                return;
            }

            // 2. 构建上下文
            String context = buildContext(searchResults);
            String prompt = buildPrompt(question, context);

            // 3. 流式调用大语言模型
            generateAnswerStream(prompt, history, callback);

        } catch (Exception e) {
            logger.error("RAG 流式查询失败", e);
            callback.onError(e);
        }
    }

    private String buildContext(List<VectorSearchService.SearchResult> searchResults) {
        StringBuilder context = new StringBuilder();
        
        for (int i = 0; i < searchResults.size(); i++) {
            VectorSearchService.SearchResult result = searchResults.get(i);
            context.append("【参考资料 ").append(i + 1).append("】\n");
            context.append(result.getContent()).append("\n\n");
        }
        
        return context.toString();
    }

    private String buildPrompt(String question, String context) {
        return String.format(
            "你是一个专业的 AI 助手。请根据以下参考资料回答用户的问题。\n\n" +
            "参考资料：\n%s\n" +
            "用户问题：%s\n\n" +
            "请基于以上参考资料给出准确、详细的回答。如果参考资料中没有相关信息，请明确说明。",
            context, question
        );
    }

    private void generateAnswerStream(String prompt, List<Map<String, String>> history, StreamCallback callback) 
            throws NoApiKeyException, ApiException, InputRequiredException {
        
        // 构建消息列表：历史消息 + 当前问题
        List<Message> messages = new ArrayList<>();
        
        for (Map<String, String> historyMsg : history) {
            String role = historyMsg.get("role");
            String content = historyMsg.get("content");
            
            if ("user".equals(role)) {
                messages.add(Message.builder()
                        .role(Role.USER.getValue())
                        .content(content)
                        .build());
            } else if ("assistant".equals(role)) {
                messages.add(Message.builder()
                        .role(Role.ASSISTANT.getValue())
                        .content(content)
                        .build());
            }
        }
        
        Message userMsg = Message.builder()
                .role(Role.USER.getValue())
                .content(prompt)
                .build();
        messages.add(userMsg);
        
        GenerationParam param = GenerationParam.builder()
                .apiKey(apiKey)
                .model(model)
                .incrementalOutput(true)  // 增量输出（流式）
                .resultFormat("message")
                .messages(messages)
                .build();

        logger.info("开始调用 AI 模型流式接口...");
        
        Flowable<GenerationResult> result = generation.streamCall(param);
        
        StringBuilder reasoningContent = new StringBuilder();
        StringBuilder finalContent = new StringBuilder();
        
        result.blockingForEach(message -> {
            if (message.getOutput() != null && 
                message.getOutput().getChoices() != null && 
                !message.getOutput().getChoices().isEmpty()) {
                
                String content = message.getOutput().getChoices().get(0).getMessage().getContent();
                
                if (content != null && !content.isEmpty()) {
                    logger.debug("收到 AI 模型内容块: {}", content);
                    finalContent.append(content);
                    callback.onContentChunk(content);
                }
            }
        });
        
        logger.info("AI 模型流式响应完成, 总内容长度: {}", finalContent.length());

        callback.onComplete(finalContent.toString(), reasoningContent.toString());
        logger.info("已调用 onComplete 回调");
    }

    /**
     * 流式回调接口
     */
    public interface StreamCallback {
        void onSearchResults(List<VectorSearchService.SearchResult> results);
        void onReasoningChunk(String chunk);
        void onContentChunk(String chunk);
        void onComplete(String fullContent, String fullReasoning);
        void onError(Exception e);
    }
}
```

**流式输出的优势**：
- **响应快**：不需要等待完整回答生成
- **用户体验好**：可以实时看到回答
- **容错性好**：网络中断时已收到的内容不会丢失

---

### 3.2 AIOps 智能运维

#### 多 Agent 协作架构
AIOps 采用 **Supervisor-Planner-Executor** 三阶段协作架构：

```
用户点击"智能运维"
  │
  ▼
AiOpsService.executeAiOpsAnalysis()
  │
  ├── 1. 构建 Planner Agent (任务规划)
  │     ├── 系统提示词: 定义角色、输出格式、报告模板
  │     ├── 工具: DateTimeTools, InternalDocsTools, QueryMetricsTools, QueryLogsTools
  │     └── 输出 Key: "planner_plan"
  │
  ├── 2. 构建 Executor Agent (任务执行)
  │     ├── 系统提示词: 执行 Planner 的第一步，返回证据摘要
  │     ├── 工具: 同上
  │     └── 输出 Key: "executor_feedback"
  │
  └── 3. 构建 Supervisor Agent (调度器)
        ├── 子 Agent: [planner_agent, executor_agent]
        ├── 最大轮次: 10
        └── 循环调度流程:
              Supervisor ──→ Planner: 分析告警，输出 decision
                    │
                    ├── decision == PLAN ──→ 继续制定计划
                    │
                    ├── decision == EXECUTE ──→ Executor: 执行第一步，返回反馈
                    │                               │
                    │                               └──→ Supervisor 再次调度 Planner
                    │
                    └── decision == FINISH ──→ Planner 输出完整报告
```

#### 关键代码详解

##### 1. Supervisor Agent 构建 - AiOpsService

```java
@Service
public class AiOpsService {

    private static final Logger logger = LoggerFactory.getLogger(AiOpsService.class);

    @Autowired
    private DateTimeTools dateTimeTools;

    @Autowired
    private InternalDocsTools internalDocsTools;

    @Autowired
    private QueryMetricsTools queryMetricsTools;

    @Autowired(required = false)  // Mock 模式下才注册
    private QueryLogsTools queryLogsTools;

    /**
     * 执行 AI Ops 告警分析流程
     */
    public Optional<OverAllState> executeAiOpsAnalysis(DashScopeChatModel chatModel, ToolCallback[] toolCallbacks) {
        logger.info("开始执行 AI Ops 多 Agent 协作流程");

        // 构建 Planner 和 Executor Agent
        ReactAgent plannerAgent = buildPlannerAgent(chatModel, toolCallbacks);
        ReactAgent executorAgent = buildExecutorAgent(chatModel, toolCallbacks);

        // 构建 Supervisor Agent
        SupervisorAgent supervisorAgent = SupervisorAgent.builder()
                .name("ai_ops_supervisor")
                .description("负责调度 Planner 与 Executor 的多 Agent 控制器")
                .model(chatModel)
                .systemPrompt(buildSupervisorSystemPrompt())
                .subAgents(List.of(plannerAgent, executorAgent))
                .build();

        String taskPrompt = "你是企业级 SRE，接到了自动化告警排查任务。请结合工具调用，执行规划→执行→再规划的闭环，并最终按照固定模板输出《告警分析报告》。禁止编造虚假数据，如连续多次查询失败需诚实反馈无法完成的原因。";

        logger.info("调用 Supervisor Agent 开始编排...");
        return supervisorAgent.invoke(taskPrompt);
    }

    /**
     * 构建 Planner Agent
     */
    private ReactAgent buildPlannerAgent(DashScopeChatModel chatModel, ToolCallback[] toolCallbacks) {
        return ReactAgent.builder()
                .name("planner_agent")
                .description("负责拆解告警、规划与再规划步骤")
                .model(chatModel)
                .systemPrompt(buildPlannerPrompt())
                .methodTools(buildMethodToolsArray())
                .tools(toolCallbacks)
                .outputKey("planner_plan")
                .build();
    }

    /**
     * 构建 Executor Agent
     */
    private ReactAgent buildExecutorAgent(DashScopeChatModel chatModel, ToolCallback[] toolCallbacks) {
        return ReactAgent.builder()
                .name("executor_agent")
                .description("负责执行 Planner 的首个步骤并及时反馈")
                .model(chatModel)
                .systemPrompt(buildExecutorPrompt())
                .methodTools(buildMethodToolsArray())
                .tools(toolCallbacks)
                .outputKey("executor_feedback")
                .build();
    }

    /**
     * 动态构建方法工具数组
     */
    private Object[] buildMethodToolsArray() {
        if (queryLogsTools != null) {
            return new Object[]{dateTimeTools, internalDocsTools, queryMetricsTools, queryLogsTools};
        } else {
            return new Object[]{dateTimeTools, internalDocsTools, queryMetricsTools};
        }
    }
```

##### 2. Planner 系统提示词 - 完整模板

```java
private String buildPlannerPrompt() {
    return """
        你是 Planner Agent，同时承担 Replanner 角色，负责：
        1. 读取当前输入任务 {input} 以及 Executor 的最近反馈 {executor_feedback}。
        2. 分析 Prometheus 告警、日志、内部文档等信息，制定可执行的下一步步骤。
        3. 在执行阶段，输出 JSON，包含 decision (PLAN|EXECUTE|FINISH)、step 描述、预期要调用的工具、以及必要的上下文。
        4. 调用任何腾讯云日志/主题相关工具时，region 参数必须使用连字符格式（如 ap-guangzhou），若不确定请省略以使用默认值。
        5. 严格禁止编造数据，只能引用工具返回的真实内容；如果连续 3 次调用同一工具仍失败或返回空结果，需停止该方向并在最终报告的结论部分说明"无法完成"的原因。

        ## 最终报告输出要求（CRITICAL）

        当 decision=FINISH 时，你必须：
        1. 不要输出 JSON 格式
        2. 直接输出完整的 Markdown 格式报告文本
        3. 报告必须严格遵循以下模板：

        # 告警分析报告

        ---

        ## 📋 活跃告警清单

        | 告警名称 | 级别 | 目标服务 | 首次触发时间 | 最新触发时间 | 状态 |
        |---------|------|----------|-------------|-------------|------|
        | [告警1名称] | [级别] | [服务名] | [时间] | [时间] | 活跃 |
        | [告警2名称] | [级别] | [服务名] | [时间] | [时间] | 活跃 |

        ---

        ## 🔍 告警根因分析 1 - [告警名称]

        ### 告警详情
        - **告警级别**: [级别]
        - **受影响服务**: [服务名]
        - **持续时间**: [X分钟]

        ### 症状描述
        [根据监控指标描述症状]

        ### 日志证据
        [引用查询到的关键日志]

        ### 根因结论
        [基于证据得出的根本原因]

        ---

        ## 🛠️ 处理方案执行 1 - [告警名称]

        ### 已执行的排查步骤
        1. [步骤1]
        2. [步骤2]

        ### 处理建议
        [给出具体的处理建议]

        ### 预期效果
        [说明预期的效果]

        ---

        ## 🔍 告警根因分析 2 - [告警名称]
        [如果有第2个告警，重复上述格式]

        ---

        ## 📊 结论

        ### 整体评估
        [总结所有告警的整体情况]

        ### 关键发现
        - [发现1]
        - [发现2]

        ### 后续建议
        1. [建议1]
        2. [建议2]

        ### 风险评估
        [评估当前风险等级和影响范围]

        ---

        ## 💡 学习资源
        [查询到的相关知识库文档内容]
        """;
}
```

**提示词设计要点**：
- 清晰的角色定位
- 明确的输出格式要求
- 结构化的报告模板
- 错误处理要求
- 严格禁止编造数据

##### 3. Executor 系统提示词

```java
private String buildExecutorPrompt() {
    return """
        你是 Executor Agent，负责读取 Planner 最新输出 {planner_plan}，只执行其中的第一步。
        - 确认步骤所需的工具与参数，尤其是 region 参数要使用连字符格式（ap-guangzhou）；若 Planner 未给出则使用默认区域。
        - 调用相应的工具并收集结果，如工具返回错误或空数据，需要将失败原因、请求参数一并记录，并停止进一步调用该工具（同一工具失败达到 3 次时应直接返回 FAILED）。
        - 将日志、指标、文档等证据整理成结构化摘要，标注对应的告警名称或资源，方便 Planner 填充"告警根因分析 / 处理方案执行"章节。
        - 以 JSON 形式返回执行状态、证据以及给 Planner 的建议，写入 executor_feedback，严禁编造未实际查询到的内容。

        输出格式示例：
        {
            "status": "SUCCESS|FAILED",
            "summary": "近1小时未见 error 日志，仅有 info",
            "evidence": "完整的工具返回内容",
            "nextHint": "建议转向高占用进程排查"
        }
        """;
}
```

##### 4. 提取最终报告

```java
public Optional<String> extractFinalReport(OverAllState state) {
    logger.info("开始提取最终报告...");

    Optional<AssistantMessage> plannerFinalOutput = state.value("planner_plan")
        .filter(AssistantMessage.class::isInstance)
        .map(AssistantMessage.class::cast);

    if (plannerFinalOutput.isPresent()) {
        String reportText = plannerFinalOutput.get().getText();
        logger.info("成功提取到 Planner 最终报告, 长度: {}", reportText.length());
        return Optional.of(reportText);
    } else {
        logger.warn("未能提取到 Planner 最终报告");
        return Optional.empty();
    }
}
```

---

### 3.3 Agent 工具集

#### 工具类说明
| 工具类 | 方法 | 描述 | 文件位置 |
|--------|------|------|---------|
| **DateTimeTools** | `getCurrentDateTime()` | 获取当前时间，用于日志时间戳参照 | [DateTimeTools.java](file:///d:/BaiduNetdiskDownload/尚硅谷/智能%20OnCall%20Agent%20项目/SuperBizAgent-release-2026-01-02/SuperBizAgent-release-2026-01-02/src/main/java/org/example/agent/tool/DateTimeTools.java) |
| **InternalDocsTools** | `queryInternalDocs(query)` | 查询内部运维文档知识库，基于向量检索 | [InternalDocsTools.java](file:///d:/BaiduNetdiskDownload/尚硅谷/智能%20OnCall%20Agent%20项目/SuperBizAgent-release-2026-01-02/SuperBizAgent-release-2026-01-02/src/main/java/org/example/agent/tool/InternalDocsTools.java) |
| **QueryMetricsTools** | `queryPrometheusAlerts()` | 查询 Prometheus 告警，支持 Mock 模式 | [QueryMetricsTools.java](file:///d:/BaiduNetdiskDownload/尚硅谷/智能%20OnCall%20Agent%20项目/SuperBizAgent-release-2026-01-02/SuperBizAgent-release-2026-01-02/src/main/java/org/example/agent/tool/QueryMetricsTools.java) |
| **QueryLogsTools** | `queryLogs(query, topic, from, to)` | 查询腾讯云 CLS 日志，支持 Mock 模式 | [QueryLogsTools.java](file:///d:/BaiduNetdiskDownload/尚硅谷/智能%20OnCall%20Agent%20项目/SuperBizAgent-release-2026-01-02/SuperBizAgent-release-2026-01-02/src/main/java/org/example/agent/tool/QueryLogsTools.java) |

#### InternalDocsTools 完整代码

```java
@Component
public class InternalDocsTools {

    private static final Logger logger = LoggerFactory.getLogger(InternalDocsTools.class);

    public static final String TOOL_QUERY_INTERNAL_DOCS = "queryInternalDocs";

    private final VectorSearchService vectorSearchService;

    @Value("${rag.top-k:3}")
    private int topK = 3;

    private final ObjectMapper objectMapper = new ObjectMapper();

    @Autowired
    public InternalDocsTools(VectorSearchService vectorSearchService) {
        this.vectorSearchService = vectorSearchService;
    }

    @Tool(description = "Use this tool to search internal documentation and knowledge base for relevant information. " +
            "It performs RAG (Retrieval-Augmented Generation) to find similar documents and extract processing steps. " +
            "This is useful when you need to understand internal procedures, best practices, or step-by-step guides " +
            "stored in the company's documentation.")
    public String queryInternalDocs(
            @ToolParam(description = "Search query describing what information you are looking for") 
            String query) {
        
        try {
            List<VectorSearchService.SearchResult> searchResults = 
                vectorSearchService.searchSimilarDocuments(query, topK);
            
            if (searchResults.isEmpty()) {
                return "{\"status\": \"no_results\", \"message\": \"No relevant documents found in the knowledge base.\"}";
            }
            
            String resultJson = objectMapper.writeValueAsString(searchResults);
            
            return resultJson;
            
        } catch (Exception e) {
            logger.error("[工具错误] queryInternalDocs 执行失败", e);
            return String.format("{\"status\": \"error\", \"message\": \"Failed to query internal docs: %s\"}", 
                    e.getMessage());
        }
    }
}
```

#### Mock 模式说明
```yaml
# application.yml
prometheus:
  base-url: http://localhost:9090
  mock-enabled: false  # true 时返回模拟告警数据，无需真实 Prometheus

cls:
  mock-enabled: false  # true 时返回模拟日志数据，无需真实腾讯云 CLS
```

#### 工具动态注册
```java
private Object[] buildMethodToolsArray() {
    if (queryLogsTools != null) {
        // Mock 模式：包含 QueryLogsTools
        return new Object[]{dateTimeTools, internalDocsTools, queryMetricsTools, queryLogsTools};
    } else {
        // 真实模式：不包含（由 MCP 协议提供）
        return new Object[]{dateTimeTools, internalDocsTools, queryMetricsTools};
    }
}
```

---

### 3.4 会话管理（短期记忆）

#### 核心实现 - ChatController

```java
@RestController
@RequestMapping("/api")
public class ChatController {

    private static final Logger logger = LoggerFactory.getLogger(ChatController.class);

    // 使用 ConcurrentHashMap 线程安全存储会话
    private final Map<String, SessionInfo> sessions = new ConcurrentHashMap<>();
    
    // 最多保留 6 对消息（用户消息 + AI 回复 = 1 对）
    private static final int MAX_WINDOW_SIZE = 6;

    // 线程池用于异步执行 SSE
    private final ExecutorService executor = Executors.newCachedThreadPool();

    /**
     * SessionInfo 内部类
     */
    private static class SessionInfo {
        private final String sessionId;
        private final List<Map<String, String>> messageHistory;
        private final long createTime;
        private final ReentrantLock lock;  // 使用可重入锁保证线程安全

        public SessionInfo(String sessionId) {
            this.sessionId = sessionId;
            this.messageHistory = new ArrayList<>();
            this.createTime = System.currentTimeMillis();
            this.lock = new ReentrantLock();
        }

        /**
         * 添加一对消息（自动裁剪）
         */
        public void addMessage(String userQuestion, String aiAnswer) {
            lock.lock();
            try {
                messageHistory.add(Map.of("role", "user", "content", userQuestion));
                messageHistory.add(Map.of("role", "assistant", "content", aiAnswer));

                int maxMessages = MAX_WINDOW_SIZE * 2;
                while (messageHistory.size() > maxMessages) {
                    messageHistory.remove(0);  // 删除最旧的用户消息
                    if (!messageHistory.isEmpty()) {
                        messageHistory.remove(0);  // 删除对应的 AI 回复
                    }
                }

                logger.debug("会话 {} 更新历史消息，当前消息对数: {}", 
                    sessionId, messageHistory.size() / 2);

            } finally {
                lock.unlock();
            }
        }

        /**
         * 获取历史（返回副本，避免并发修改问题）
         */
        public List<Map<String, String>> getHistory() {
            lock.lock();
            try {
                return new ArrayList<>(messageHistory);
            } finally {
                lock.unlock();
            }
        }

        /**
         * 清空历史
         */
        public void clearHistory() {
            lock.lock();
            try {
                messageHistory.clear();
                logger.info("会话 {} 历史消息已清空", sessionId);
            } finally {
                lock.unlock();
            }
        }

        public int getMessagePairCount() {
            lock.lock();
            try {
                return messageHistory.size() / 2;
            } finally {
                lock.unlock();
            }
        }
    }

    /**
     * 获取或创建会话
     */
    private SessionInfo getOrCreateSession(String sessionId) {
        if (sessionId == null || sessionId.isEmpty()) {
            sessionId = UUID.randomUUID().toString();
        }
        return sessions.computeIfAbsent(sessionId, SessionInfo::new);
    }
}
```

#### 会话历史注入 Prompt

```java
public String buildSystemPrompt(List<Map<String, String>> history) {
    StringBuilder sb = new StringBuilder();
    sb.append("你是一个专业的智能助手，可以获取当前时间、搜索内部文档知识库、以及查询 Prometheus 告警信息。\n");
    sb.append("当用户询问时间相关问题时，使用 getCurrentDateTime 工具。\n");
    sb.append("当用户需要查询公司内部文档、流程、最佳实践或技术指南时，使用 queryInternalDocs 工具。\n");
    sb.append("当用户需要查询 Prometheus 告警、监控指标或系统状态时，使用 queryPrometheusAlerts 工具。\n");
    sb.append("当用户需要查询腾讯云日志时，请调用腾讯云 MCP 服务查询，默认查询地域 ap-guangzhou，查询时间范围为近一个月。\n\n");

    if (!history.isEmpty()) {
        sb.append("--- 对话历史 ---\n");
        for (Map<String, String> msg : history) {
            if ("user".equals(msg.get("role"))) {
                sb.append("用户: ").append(msg.get("content")).append("\n");
            } else {
                sb.append("助手: ").append(msg.get("content")).append("\n");
            }
        }
        sb.append("--- 对话历史结束 ---\n\n");
    }

    sb.append("请基于以上对话历史，回答用户的新问题。");
    return sb.toString();
}
```

---

### 3.5 SSE 流式输出

#### SSE 输出流程
```java
@PostMapping(value = "/chat_stream", produces = "text/event-stream;charset=UTF-8")
public SseEmitter chatStream(@RequestBody ChatRequest request) {
    // 1. 创建 SSE 发射器，设置 5 分钟超时
    SseEmitter emitter = new SseEmitter(300000L);

    // 2. 参数校验
    if (request.getQuestion() == null || request.getQuestion().trim().isEmpty()) {
        try {
            emitter.send(SseEmitter.event().name("message").data(SseMessage.error("问题内容不能为空"), MediaType.APPLICATION_JSON));
            emitter.complete();
        } catch (IOException e) {
            emitter.completeWithError(e);
        }
        return emitter;
    }

    // 3. 异步执行，不阻塞主线程
    executor.execute(() -> {
        try {
            logger.info("收到 ReactAgent 对话请求 - SessionId: {}, Question: {}", request.getId(), request.getQuestion());

            SessionInfo session = getOrCreateSession(request.getId());
            List<Map<String, String>> history = session.getHistory();

            DashScopeApi dashScopeApi = chatService.createDashScopeApi();
            DashScopeChatModel chatModel = chatService.createStandardChatModel(dashScopeApi);

            chatService.logAvailableTools();

            String systemPrompt = chatService.buildSystemPrompt(history);
            ReactAgent agent = chatService.createReactAgent(chatModel, systemPrompt);

            // 4. 使用流式调用
            StringBuilder fullAnswerBuilder = new StringBuilder();
            Flux<NodeOutput> stream = agent.stream(request.getQuestion());

            stream.subscribe(
                output -> {  // 数据推送回调
                    try {
                        if (output instanceof StreamingOutput streamingOutput) {
                            OutputType type = streamingOutput.getOutputType();

                            if (type == OutputType.AGENT_MODEL_STREAMING) {
                                String chunk = streamingOutput.message().getText();
                                if (chunk != null && !chunk.isEmpty()) {
                                    fullAnswerBuilder.append(chunk);
                                    emitter.send(SseEmitter.event()
                                            .name("message")
                                            .data(SseMessage.content(chunk), MediaType.APPLICATION_JSON));
                                    logger.info("发送流式内容: {}", chunk);
                                }
                            }
                        }
                    } catch (IOException e) {
                        logger.error("发送流式消息失败", e);
                        throw new RuntimeException(e);
                    }
                },
                error -> {  // 错误处理回调
                    logger.error("ReactAgent 流式对话失败", error);
                    try {
                        emitter.send(SseEmitter.event()
                                .name("message")
                                .data(SseMessage.error(error.getMessage()), MediaType.APPLICATION_JSON));
                    } catch (IOException ex) {
                        logger.error("发送错误消息失败", ex);
                    }
                    emitter.completeWithError(error);
                },
                () -> {  // 完成回调
                    try {
                        String fullAnswer = fullAnswerBuilder.toString();
                        session.addMessage(request.getQuestion(), fullAnswer);
                        emitter.send(SseEmitter.event()
                                .name("message")
                                .data(SseMessage.done(), MediaType.APPLICATION_JSON));
                        emitter.complete();
                    } catch (IOException e) {
                        logger.error("发送完成消息失败", e);
                        emitter.completeWithError(e);
                    }
                }
            );

        } catch (Exception e) {
            logger.error("ReactAgent 对话初始化失败", e);
            try {
                emitter.send(SseEmitter.event()
                        .name("message")
                        .data(SseMessage.error(e.getMessage()), MediaType.APPLICATION_JSON));
            } catch (IOException ex) {
                logger.error("发送错误消息失败", ex);
            }
            emitter.completeWithError(e);
        }
    });

    return emitter;
}
```

---

## 四、API 接口文档

### 4.1 接口列表

| 接口 | 方法 | 功能 | 返回类型 |
|------|------|------|---------|
| `POST /api/chat` | POST | 普通对话（一次性返回） | JSON |
| `POST /api/chat_stream` | POST | 流式对话（SSE） | SSE |
| `POST /api/ai_ops` | POST | 智能运维分析（SSE） | SSE |
| `POST /api/upload` | POST | 上传文档并自动向量化 | JSON |
| `POST /api/chat/clear` | POST | 清空会话历史 | JSON |
| `GET /api/chat/session/{sessionId}` | GET | 获取会话信息 | JSON |
| `GET /milvus/health` | GET | Milvus 健康检查 | JSON |

### 4.2 接口详情

#### 普通对话 - `/api/chat`

```json
// 请求
POST /api/chat
Content-Type: application/json

{
  "id": "session-001",
  "question": "CPU 使用率过高怎么排查？"
}

// 响应
{
  "code": 200,
  "message": "success",
  "data": {
    "success": true,
    "answer": "排查 CPU 使用率过高的步骤如下：\n1. 使用 top 命令查看进程 CPU 占用\n2. 检查是否有死循环\n3. 查看 JVM 线程栈（如有 Java 应用）\n4. 检查 Prometheus 监控指标历史趋势",
    "errorMessage": null
  }
}
```

#### 流式对话 - `/api/chat_stream`

```json
// 请求
POST /api/chat_stream
Content-Type: application/json

{
  "id": "session-001",
  "question": "CPU 使用率过高怎么排查？"
}

// SSE 响应（逐块推送）
event: message
data: {"type":"content","data":"排查 CPU 使用率过高的步骤如下："}

event: message
data: {"type":"content","data":"\n1. 使用 top 命令查看进程"}

event: message
data: {"type":"content","data":" CPU 占用"}

event: message
data: {"type":"done","data":null}
```

#### 智能运维 - `/api/ai_ops`

```json
// 请求（无需参数）
POST /api/ai_ops

// SSE 响应
event: message
data: {"type":"content","data":"正在读取告警并拆解任务...\n"}

event: message
data: {"type":"content","data":"# 告警分析报告\n---\n## 📋 活跃告警清单..."}

event: message
data: {"type":"done","data":null}
```

#### 上传文档 - `/api/upload`

```
// 请求
POST /api/upload
Content-Type: multipart/form-data
file: 运维文档.txt

// 响应
{
  "code": 200,
  "message": "success",
  "data": "文档上传并处理成功，共切分为 15 个片段，已存入向量数据库"
}
```

---

## 五、配置说明

### 5.1 application.yml 完整配置

```yaml
server:
  port: 9900

# Milvus 向量数据库配置
milvus:
  host: 192.168.159.128
  port: 19530
  username: ""
  password: ""
  database: default
  timeout: 5000

# Spring AI - DashScope
spring:
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY:sk-167e979c2a59414f9791711711bacade}
      chat:
        options:
          model: qwen3-max
          temperature: 0.7
          max-tokens: 2000
          top-p: 0.9
          timeout: 180000
      retry:
        max-attempts: 3
        backoff-delay: 1000
    # MCP 配置（如不使用，可注释掉）
    # mcp:
    #   client:
    #     enabled: false

# Embedding 向量化配置
dashscope:
  api:
    key: ${DASHSCOPE_API_KEY:sk-167e979c2a59414f9791711711bacade}
  embedding:
    model: text-embedding-v4

# RAG 检索配置
rag:
  top-k: 3
  model: qwen3-30b-a3b-thinking-2507

# 文档分片配置
document:
  chunk:
    max-size: 800
    overlap: 100

# 上传配置
file:
  upload:
    path: ./uploads
    allowed-extensions: txt,md

# Prometheus 配置
prometheus:
  base-url: http://localhost:9090
  mock-enabled: false

# CLS 配置
cls:
  mock-enabled: false
```

---

## 六、关键细节

### 6.1 Spring AI @Tool 注解

```java
@Tool(description = "Use this tool to search internal documentation and knowledge base for relevant information.")
public String queryInternalDocs(
        @ToolParam(description = "Search query describing what information you are looking for") 
        String query) {
    // 实现逻辑
}
```

**注解说明**：
| 注解 | 说明 |
|------|------|
| `@Tool` | 标记方法为 Agent 可调用工具，description 告诉 Agent 这个工具的用途 |
| `@ToolParam` | 描述参数，Agent 根据描述决定如何传参 |

### 6.2 ReactAgent 使用

```java
ReactAgent agent = ReactAgent.builder()
    .name("intelligent_assistant")
    .model(chatModel)
    .systemPrompt(systemPrompt)
    .methodTools(dateTimeTools, internalDocsTools, queryMetricsTools)
    .tools(tools.getToolCallbacks())
    .build();

// 非流式调用
String answer = agent.call("现在几点？");

// 流式调用
Flux<NodeOutput> stream = agent.stream("现在几点？");
```

### 6.3 SupervisorAgent 多 Agent 协作

```java
SupervisorAgent supervisor = SupervisorAgent.builder()
    .name("ai_ops_supervisor")
    .description("负责调度 Planner 与 Executor 的多 Agent 控制器")
    .model(chatModel)
    .systemPrompt(buildSupervisorSystemPrompt())
    .subAgents(List.of(plannerAgent, executorAgent))
    .build();

// invoke 会自动循环调度子 Agent，直到任务完成
Optional<OverAllState> state = supervisor.invoke("分析告警");

// 从 state 中提取结果
Optional<String> report = aiOpsService.extractFinalReport(state.get());
```

---

## 七、常见问题

### 7.1 MCP 客户端占位符导致启动失败

**现象**：启动时报错，说连接不到 MCP 服务

**解决**：注释掉 `application.yml` 中的 `spring.ai.mcp.*` 配置，或改为 `enabled: false`

```yaml
spring:
  ai:
    # mcp:
    #   client:
    #     enabled: false
```

### 7.2 Milvus Collection 重复创建

**现象**：每次启动都尝试创建 biz Collection，报错已存在

**解决**：在 `VectorIndexService` 中先检查是否存在

```java
R<String> response = milvusClient.listCollections(
    ListCollectionsParam.newBuilder()
        .withDatabaseName(MilvusConstants.MILVUS_DATABASE_NAME)
        .build()
);
if (!response.getData().contains(MilvusConstants.MILVUS_COLLECTION_NAME)) {
    milvusClient.createCollection(...);
}
```

### 7.3 API Key 配置不一致

**现象**：DashScope 调用失败，或向量化失败

**解决**：确保 `spring.ai.dashscope.api-key` 和 `dashscope.api.key` 使用相同的环境变量引用

```yaml
spring:
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY:sk-xxx}

dashscope:
  api:
    key: ${DASHSCOPE_API_KEY:sk-xxx}
```

### 7.4 网络连通性

**现象**：连接不到 Milvus 或 DashScope

**解决**：
```powershell
# Windows 测试 Milvus 连接
Test-NetConnection -ComputerName 192.168.159.128 -Port 19530

# Linux/Mac 测试
nc -zv 192.168.159.128 19530
```

---

## 八、扩展方向

| 方向 | 实现思路 |
|------|---------|
| 新增 Agent 工具 | 加 `@Tool` 注解即可，Agent 自动发现 |
| 切换大模型 | 改配置 `spring.ai.dashscope.chat.options.model` |
| 切换向量库 | 实现 `VectorSearchService` 接口 |
| 多租户 | SessionId 关联 TenantId，Milvus 按租户分区 |
| 会话持久化 | 使用 Redis 存储历史，支持服务重启恢复 |
| 权限控制 | Spring Security + JWT 认证 |
| 监控 | Prometheus + Grafana 监控接口性能和调用次数 |
| 测试框架 | 添加单元测试和集成测试 |
| 日志追踪 | 使用 Trace ID 追踪调用链路 |
| 多模态支持 | 添加图像理解模型，分析监控截图 |
| 反馈系统 | 用户反馈纠正，持续优化模型回答质量 |

---

## 总结

SuperBizAgent 项目展示了如何将 Spring AI、向量数据库、大语言模型等技术结合起来，构建一个完整的 AIOps 系统。项目代码结构清晰，适合学习和扩展。

---

*文档生成时间：2026-04-27*
